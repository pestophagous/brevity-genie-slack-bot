package util

// choose and verify a behavior for when string is unparseable

// research how slack api uses time ZONES

// research how slack api uses nanosecond. (feed the other digits to time.Unix)

// make this actually return UTC. (time.Unix does local, so the func name is a misnomer)

// consider adding 'Timely' to the func name and CHECKING FOR A RECENT TIME
